import { CountrysearchPipe } from './countrysearch.pipe';

describe('CountrysearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CountrysearchPipe();
    expect(pipe).toBeTruthy();
  });
});
